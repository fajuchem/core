@using Core.Base.Leiautes
<Router AppAssembly="@typeof(Program).Assembly" PreferExactMatches="@true">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(LeiautePrincipal)" />
    </Found>
    <NotFound>
        <LayoutView Layout="@typeof(LeiautePrincipal)">
            <p>Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
